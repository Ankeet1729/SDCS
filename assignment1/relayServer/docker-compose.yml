version: '3'
services:
  relay:
    build:
      context: ./
      dockerfile: Dockerfile.relay
    ports:
      - "8000:8000"
    depends_on:
      - dump
    networks:
      - mynetwork

  dump:
    build:
      context: ./
      dockerfile: Dockerfile.dump
    ports:
      - "9000:9000"
    networks:
      - mynetwork

networks:
  mynetwork:
